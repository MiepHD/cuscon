apply plugin: 'com.android.application'

android {
    dependenciesInfo {
        includeInApk = false
        includeInBundle = false
    }
    compileSdk = rootProject.ext.CompileSdk

    defaultConfig {
        applicationId 'com.froxot.cuscon'
        minSdkVersion rootProject.ext.MinSdk
        targetSdkVersion rootProject.ext.TargetSdk
        versionCode 4112
        versionName '4.1.1.2'
        multiDexEnabled true
    }

    bundle {
        language { enableSplit false }
    }

    signingConfigs {
        release {
            storeFile file("upload-keystore.jks")
            storePassword findProperty("PASSWORD")
            keyAlias 'upload'
            keyPassword findProperty("PASSWORD")
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            debuggable false
            minifyEnabled true
            shrinkResources = true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }

    lint {
        abortOnError false
    }
    
    flavorDimensions "variant"
	productFlavors {
		play {
			dimension "variant"
			resValue "string", "app_name", "cuscon"
			matchingFallbacks = ["play"]
		}
		foss {
			dimension "variant"
			applicationIdSuffix '.foss'
			resValue "string", "app_name", "cuscon"
			matchingFallbacks = ["foss"]
		}
	}
    namespace 'com.froxot.cuscon'
}

base {
    archivesName = "${parent.name}-v${android.defaultConfig.versionCode}"
}

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(21)
    }
}

dependencies {
	playImplementation 'com.github.MiepHD.candybar:library:3.23.0.7'
	fossImplementation 'com.github.MiepHD:candybar-foss:3.23.0.7'
}

tasks.register('finishXMLs', Exec) {
    commandLine 'bash', './finishXMLs.sh'
}

tasks.register('generatedpis', Exec) {
    commandLine 'python3', './generatedpis.py'
}

// Erzwinge, dass die Build-Tasks erst NACH finishXMLs laufen
['assembleFossRelease', 'bundlePlayRelease', 'assemblePlayRelease'].each { taskName ->
    tasks.matching { it.name == taskName }.configureEach {
        mustRunAfter 'finishXMLs', 'clean'
    }
}

tasks.register('buildRelease') {
    dependsOn 'clean', 'finishXMLs', 'assembleFossRelease', 'bundlePlayRelease', 'assemblePlayRelease'
}

// Ermitteln, ob es sich um einen Bundle-Build (aab) oder APK-Build handelt
def isBundleTask = gradle.startParameter.taskNames.any { it.toLowerCase().contains("bundle") }
def isApkTask = gradle.startParameter.taskNames.any { it.toLowerCase().contains("assemble") }

if (isBundleTask) {
    // Beim Erstellen eines Bundles (.aab) den nodpi-Ordner ausschließen
    android.sourceSets.main.res.exclude 'drawable-nodpi/**'
} else if (isApkTask) {
    // Beim Erstellen einer APK (.apk) die spezifischen DPI-Ordner ausschließen
    android.sourceSets.main.res.exclude 'drawable-hdpi/**'
    android.sourceSets.main.res.exclude 'drawable-mdpi/**'
    android.sourceSets.main.res.exclude 'drawable-xhdpi/**'
    android.sourceSets.main.res.exclude 'drawable-xxhdpi/**'
    android.sourceSets.main.res.exclude 'drawable-xxxhdpi/**'
}